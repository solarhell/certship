// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/solarhell/certship/pkg/ent/authtoken"
)

// AuthToken is the model entity for the AuthToken schema.
type AuthToken struct {
	config `json:"-"`
	// ID of the ent.
	// 主键 UUID
	ID string `json:"id,omitempty"`
	// 管理员 ID
	AdminID string `json:"admin_id,omitempty"`
	// Token
	Token string `json:"token,omitempty"`
	// 登录时的 User-Agent
	LoginUserAgent string `json:"login_user_agent,omitempty"`
	// 登录时的 IP
	LoginIP string `json:"login_ip,omitempty"`
	// 登录时间
	LoginTime time.Time `json:"login_time,omitempty"`
	// 最后活跃的 User-Agent
	LastActiveUserAgent string `json:"last_active_user_agent,omitempty"`
	// 最后活跃的 IP
	LastActiveIP string `json:"last_active_ip,omitempty"`
	// 最后活跃时间
	LastActiveTime time.Time `json:"last_active_time,omitempty"`
	selectValues   sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*AuthToken) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case authtoken.FieldID, authtoken.FieldAdminID, authtoken.FieldToken, authtoken.FieldLoginUserAgent, authtoken.FieldLoginIP, authtoken.FieldLastActiveUserAgent, authtoken.FieldLastActiveIP:
			values[i] = new(sql.NullString)
		case authtoken.FieldLoginTime, authtoken.FieldLastActiveTime:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the AuthToken fields.
func (_m *AuthToken) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case authtoken.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case authtoken.FieldAdminID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field admin_id", values[i])
			} else if value.Valid {
				_m.AdminID = value.String
			}
		case authtoken.FieldToken:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field token", values[i])
			} else if value.Valid {
				_m.Token = value.String
			}
		case authtoken.FieldLoginUserAgent:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field login_user_agent", values[i])
			} else if value.Valid {
				_m.LoginUserAgent = value.String
			}
		case authtoken.FieldLoginIP:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field login_ip", values[i])
			} else if value.Valid {
				_m.LoginIP = value.String
			}
		case authtoken.FieldLoginTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field login_time", values[i])
			} else if value.Valid {
				_m.LoginTime = value.Time
			}
		case authtoken.FieldLastActiveUserAgent:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field last_active_user_agent", values[i])
			} else if value.Valid {
				_m.LastActiveUserAgent = value.String
			}
		case authtoken.FieldLastActiveIP:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field last_active_ip", values[i])
			} else if value.Valid {
				_m.LastActiveIP = value.String
			}
		case authtoken.FieldLastActiveTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_active_time", values[i])
			} else if value.Valid {
				_m.LastActiveTime = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the AuthToken.
// This includes values selected through modifiers, order, etc.
func (_m *AuthToken) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this AuthToken.
// Note that you need to call AuthToken.Unwrap() before calling this method if this AuthToken
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *AuthToken) Update() *AuthTokenUpdateOne {
	return NewAuthTokenClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the AuthToken entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *AuthToken) Unwrap() *AuthToken {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: AuthToken is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *AuthToken) String() string {
	var builder strings.Builder
	builder.WriteString("AuthToken(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("admin_id=")
	builder.WriteString(_m.AdminID)
	builder.WriteString(", ")
	builder.WriteString("token=")
	builder.WriteString(_m.Token)
	builder.WriteString(", ")
	builder.WriteString("login_user_agent=")
	builder.WriteString(_m.LoginUserAgent)
	builder.WriteString(", ")
	builder.WriteString("login_ip=")
	builder.WriteString(_m.LoginIP)
	builder.WriteString(", ")
	builder.WriteString("login_time=")
	builder.WriteString(_m.LoginTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("last_active_user_agent=")
	builder.WriteString(_m.LastActiveUserAgent)
	builder.WriteString(", ")
	builder.WriteString("last_active_ip=")
	builder.WriteString(_m.LastActiveIP)
	builder.WriteString(", ")
	builder.WriteString("last_active_time=")
	builder.WriteString(_m.LastActiveTime.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// AuthTokens is a parsable slice of AuthToken.
type AuthTokens []*AuthToken
