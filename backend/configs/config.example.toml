# certship 配置文件
# 仅需填写数据库连接信息，其余配置（调度、下线判定、DNS 解析器、云账号、通知渠道）
# 均在 Web UI 中管理并存入数据库——写在这里的不会生效。
# 首次启动会自动创建配置行并落默认值，不需要手工往库里插数据。

[database]
host = "127.0.0.1"
port = 5432
username = "certship"
password = ""
db = "certship"
